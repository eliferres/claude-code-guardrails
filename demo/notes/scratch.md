# Scratch

An ordinary working file. Nothing protects it, so the file lock ignores writes
here — but the claims guard still tracks who is writing, because two sessions
editing one scratch file lose work exactly the same way.
